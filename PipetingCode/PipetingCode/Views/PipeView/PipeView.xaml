<UserControl x:Class="PipetitngCode.Views.PipeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PipetitngCode.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <local:PipeViewModel></local:PipeViewModel>
    </UserControl.DataContext>
    <StackPanel HorizontalAlignment="Center">
       <Grid>
           <Grid.RowDefinitions>
               <RowDefinition/>
               <RowDefinition/>
           </Grid.RowDefinitions>
           <Grid.ColumnDefinitions>
               <ColumnDefinition Width="30"/>
                <ColumnDefinition/>
           </Grid.ColumnDefinitions>

            <TextBlock Text="IP：" Grid.Row="0" Grid.Column="0" Margin="4"/>
            <TextBox Text="{Binding Ip}" Width="100" Grid.Row="0" Grid.Column="1" Margin="4"></TextBox>


            <TextBlock Text="端口："  Grid.Row="1" Grid.Column="0" Margin="4"/>
            <TextBox Text="{Binding Port}" Width="100" Grid.Row="1" Grid.Column="1" Margin="4"></TextBox>
       </Grid>
        <Button Content="连接" Command="{Binding }" Width="100" Margin="4"></Button>
    </StackPanel>
</UserControl>
